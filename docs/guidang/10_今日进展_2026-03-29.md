# 10 今日进展（2026-03-29）

## 已完成

1. 完成工作区安全边界落地（仅 howard 账户路径，重资源落盘 `/data`）。
2. 安装 Miniconda 到 `/data/howard/miniconda3`。
3. 安装用户级 tmux（用于长任务后台执行）。
4. 创建并规范化目录结构：`docs/`、`scripts/`、`configs/`、`third_party/`、`runs/`。
5. 拉取官方仓库（commit: `e996e22`）并通过软链接接入 `third_party/`。
6. 重排 `给学弟的思路.md` 为执行版，并归档原始版本。
7. 建立文档体系（00~09）和脚本入口说明。
8. 建立外层工程脚本：环境安装、健康检查、tmux执行、smoke test、数据下载/链接。
9. 完成环境兼容修复：将 torch 切换为 `2.5.1+cu124` 并解决 `LD_LIBRARY_PATH` 覆盖问题。
10. 环境健康检查通过（GPU 可用、关键依赖可导入）。
11. 拉取并对齐 `splatfacto` 检查点路径，完成 checkpoint 读取链路打通。
12. 在数据不完整（29/50 图片）条件下，使用 `colmap/sparse` 生成 `transforms.json + sparse_pc.ply`。
13. 通过 tmux 完成一次 SplatPlan smoke 运行并产出结果 JSON：
   `runs/2026-03-29_1346_splatplan_smoke_retry_partial/splatplan_smoke_output.json`。
14. 完成 `images_2` 子集 50/50 图片补齐（自动重试下载）。
15. 基于 `images_2` 构建独立场景副本 `splatnav11202024_images2`，并完成 50 帧 smoke：
   `runs/2026-03-29_1711_splatplan_smoke_old_union_sfc-1_tmux/splatplan_smoke_output.json`。
16. 完成官方风格批跑脚本与自动出图脚本，并通过小样本自测（可行率结果已输出）。
17. 已启动 12 小时夜间连续任务：
   `runs/2026-03-29_1720_overnight_12h_formal/stdout.log`。

## 进行中

1. 继续尝试补齐官方场景剩余图片（Google Drive 文件访问限制导致中断）。
2. 进行 `PrimalInfeasible` 的规划参数排查（场景边界/起终点/method 组合）。

## 下一步

1. 优先补齐 `splatnav11202024` 全量图片与官方 `transforms.json`（目标 50/50）。
2. 用全量官方数据再跑一次 smoke，目标是可行解（`feasible=True`）或明确不可行原因。
3. 固化“官方数据完整复现 SOP + 失败回退 SOP（partial）”文档。
4. 进入下一阶段：按月度目标拆分学弟任务包（环境/数据/运行/读码四线并行）。
